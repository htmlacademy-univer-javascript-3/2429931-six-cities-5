export const numberRentalOffes = 10;

export enum AppPath {
  Main = '/',
  Login = '/login',
  Favorites = '/favorites',
  Offers = '/offers',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
